# Optional — unset to use the free keyless tier (0.5 RPS).
# Get a key at https://developers.jup.ag/portal
JUP_API_KEY=

# Optional — your Solana wallet public key, only needed for --live mode.
# jup-policy never signs or submits transactions without an explicit flag.
JUP_WALLET_PUBKEY=
